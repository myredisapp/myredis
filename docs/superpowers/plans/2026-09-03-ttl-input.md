# TTL 输入框实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在「新增 Key」对话框和 Key 详情页增加 TTL（秒）输入框，默认 -1（永不过期），后端 `set_key` 原子化支持 `SET ... EX ttl`。

**Architecture:** 扩展后端 `set_key` Tauri Command 接收 `ttl: i64` 参数；`ttl > 0` 时使用 `SET key value EX ttl`，`ttl == -1` 时使用普通 `SET key value`。前端在创建/编辑 Key 的表单中增加 TTL 输入，统一做输入校验后随 `set_key` 一起提交。

**Tech Stack:** Tauri 2, Rust 2021, redis-rs, 原生 HTML/CSS/JS

## Global Constraints

- 兼容 Redis < 6.0，因此使用 `SET ... EX`（Redis 2.6.12+）。
- 前端使用原生 JS，不引入新依赖。
- TTL 仅允许 `-1` 或正整数；`0` 和小数拒绝。
- 后端禁止 `unwrap()`/`panic!`，错误用 `String` 返回给前端。
- 提交前运行 `cargo clippy -- -D warnings` 和 `cargo fmt --check`。

---

## 文件结构

| 文件 |  responsibility |
|------|----------------|
| `src-tauri/src/commands/key.rs` | 修改 `set_key`，增加 `ttl` 参数与分支逻辑 |
| `frontend/index.html` | 新增 TTL 输入框、校验逻辑、调用时传参 |

---

## Task 1: 后端 `set_key` 支持 TTL

**Files:**
- Modify: `src-tauri/src/commands/key.rs:74-91`

**Interfaces:**
- Consumes: 现有 `Pool::manager` 与 `Pool::ensure_writable` 签名不变。
- Produces: `set_key` 新签名：
  ```rust
  pub async fn set_key(
      pool: tauri::State<'_, Pool>,
      conn_id: String,
      key: String,
      value: String,
      ttl: i64,
  ) -> Result<String, String>
  ```

- [ ] **Step 1: 修改 `set_key` 签名并添加校验**

```rust
#[tauri::command]
pub async fn set_key(
    pool: tauri::State<'_, Pool>,
    conn_id: String,
    key: String,
    value: String,
    ttl: i64,
) -> Result<String, String> {
    if ttl != -1 && ttl <= 0 {
        return Err("TTL 必须为 -1 或正整数".into());
    }

    pool.ensure_writable(&conn_id).map_err(|e| e.to_string())?;
    let mut con = pool.manager(&conn_id).map_err(|e| e.to_string())?;

    let ok: String = if ttl > 0 {
        redis::cmd("SET")
            .arg(&key)
            .arg(&value)
            .arg("EX")
            .arg(ttl)
            .query_async(&mut con)
            .await
            .map_err(|e: redis::RedisError| e.to_string())?
    } else {
        redis::cmd("SET")
            .arg(&key)
            .arg(&value)
            .query_async(&mut con)
            .await
            .map_err(|e: redis::RedisError| e.to_string())?
    };

    Ok(ok)
}
```

- [ ] **Step 2: 编译检查**

Run: `cd /Users/yanshili/me/projects/myredis/src-tauri && cargo check`

Expected: 通过（可能会报前端调用点参数不匹配，但本任务只改后端）。

- [ ] **Step 3: 运行 Clippy 与格式检查**

Run:
```bash
cargo clippy -- -D warnings
cargo fmt --check
```

Expected: 无警告，无格式差异。

- [ ] **Step 4: 提交**

```bash
git add src-tauri/src/commands/key.rs
git commit -m "feat(key): set_key 支持 TTL 参数，使用 SET ... EX 原子设置"
```

---

## Task 2: 前端新增 Key 对话框增加 TTL 输入框

**Files:**
- Modify: `frontend/index.html:1631-1647`（对话框 HTML）
- Modify: `frontend/index.html:2747-2809`（对话框 JS 逻辑）

**Interfaces:**
- Consumes: `set_key` 现在需要 `ttl` 字段。
- Produces: 新增 DOM 元素 `addKeyTtl`，新增辅助函数 `parseTtl(input)`。

- [ ] **Step 1: 在对话框 HTML 中增加 TTL 输入框**

在 `frontend/index.html:1640`（值文本域下方）插入：

```html
            <div class="form-group">
                <label>TTL（秒）</label>
                <input type="number" id="addKeyTtl" value="-1" placeholder="-1 表示永不过期" />
                <small style="color: var(--text-muted); font-size: 12px;">-1 表示永不过期，正整数表示过期秒数</small>
            </div>
```

- [ ] **Step 2: 在 JS 中获取 TTL 元素并重置默认值**

在 `openAddKeyModal` 附近（约 2748-2764 行）修改：

```js
            const addKeyModal = document.getElementById('addKeyModal');
            const addKeyNameInput = document.getElementById('addKeyName');
            const addKeyValueInput = document.getElementById('addKeyValue');
            const addKeyTtlInput = document.getElementById('addKeyTtl');

            function openAddKeyModal() {
                if (!currentConn || !currentConn.id) {
                    showToast('请先连接 Redis 服务器', 'error');
                    return;
                }
                if (currentConn.online !== true) {
                    showToast('请先连接 Redis 服务器', 'error');
                    return;
                }
                addKeyNameInput.value = '';
                addKeyValueInput.value = '';
                addKeyTtlInput.value = '-1';
                addKeyModal.classList.add('open');
                setTimeout(() => addKeyNameInput.focus(), 50);
            }
```

- [ ] **Step 3: 增加 TTL 解析与校验辅助函数**

在 `submitAddKey` 之前添加：

```js
            function parseTtl(value) {
                if (value === '' || value === null || value === undefined) {
                    return { ok: false, message: '请输入 TTL' };
                }
                const trimmed = String(value).trim();
                if (!/^-?\d+$/.test(trimmed)) {
                    return { ok: false, message: 'TTL 必须为整数' };
                }
                const n = parseInt(trimmed, 10);
                if (n === -1) {
                    return { ok: true, ttl: -1 };
                }
                if (n <= 0) {
                    return { ok: false, message: 'TTL 必须为 -1 或正整数' };
                }
                return { ok: true, ttl: n };
            }
```

- [ ] **Step 4: 修改 `submitAddKey` 以校验并提交 TTL**

将 `submitAddKey` 替换为：

```js
            function submitAddKey() {
                const trimmed = addKeyNameInput.value.trim();
                if (!trimmed) {
                    showToast('请输入 Key 名称', 'error');
                    addKeyNameInput.focus();
                    return;
                }
                if (allKeys.some(k => k.key === trimmed)) {
                    showToast('Key 已存在', 'error');
                    return;
                }
                const ttlResult = parseTtl(addKeyTtlInput.value);
                if (!ttlResult.ok) {
                    showToast(ttlResult.message, 'error');
                    addKeyTtlInput.focus();
                    return;
                }
                const value = addKeyValueInput.value;
                document.getElementById('btnAddKey').disabled = true;
                invoke('set_key', { connId: currentConn.id, key: trimmed, value: value, ttl: ttlResult.ttl })
                    .then(() => {
                        closeAddKeyModal();
                        showToast(`Key "${trimmed}" 已创建`, 'success');
                        const ttlArg = ttlResult.ttl > 0 ? ` EX ${ttlResult.ttl}` : '';
                        appendTerminal(`SET ${trimmed} "${value}"${ttlArg} → OK`, 'ok');
                        loadKeys().then(() => {
                            selectedKey = trimmed;
                            renderKeyTree(keySearch.value);
                            renderDetail();
                        });
                    })
                    .catch(err => {
                        showToast('创建失败: ' + err, 'error');
                        appendTerminal(`SET ${trimmed} 失败: ${err}`, 'error');
                    })
                    .finally(() => {
                        document.getElementById('btnAddKey').disabled = false;
                    });
            }
```

- [ ] **Step 5: 增加 Enter 键在 TTL 输入框的提交行为**

在事件监听部分（约 2807-2808 行）添加：

```js
            addKeyTtlInput.addEventListener('keydown', (e) => { if (e.key === 'Enter') submitAddKey(); });
```

- [ ] **Step 6: 验证 HTML 结构**

Run: 在浏览器或 Tauri 中打开「新增 Key」对话框，确认：
- 默认 TTL 为 `-1`。
- 输入 `0`、`-2`、`1.5`、`abc` 时点创建会提示错误。
- 输入 `60` 可以提交。

- [ ] **Step 7: 提交**

```bash
git add frontend/index.html
git commit -m "feat(ui): 新增 Key 对话框增加 TTL 输入框"
```

---

## Task 3: Key 详情页支持编辑 TTL

**Files:**
- Modify: `frontend/index.html:897-905`（CSS，可选，为 TTL 输入框保持一致样式）
- Modify: `frontend/index.html:2364-2372`（详情页 TTL 展示改为输入框）
- Modify: `frontend/index.html:2413-2431`（保存时读取并提交 TTL）

**Interfaces:**
- Consumes: `parseTtl` 辅助函数（Task 2 已定义）。
- Produces: 详情页 DOM 元素 `detailTtl`，保存时一并提交 `ttl`。

- [ ] **Step 1: 为详情页 TTL 输入框增加 CSS**

在 `<style>` 中（约 897 行 `.detail-header .ttl` 附近）追加：

```css
        .detail-header .ttl input {
            background: transparent;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            color: var(--text-primary);
            padding: 2px 8px;
            font-size: 13px;
            width: 80px;
            outline: none;
        }
        .detail-header .ttl input:focus {
            border-color: var(--accent);
        }
        .detail-header .ttl input:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
```

- [ ] **Step 2: 将详情页 TTL 展示改为可编辑输入框**

将 `frontend/index.html:2364-2372` 区域替换为：

```js
                const ttlDisplay = ttl === -1 ? '永不过期' : `${ttl}s`;
                const ttlDisabled = readonlyConn ? ' disabled' : '';

                keyDetailEl.innerHTML = `
                    <div class="detail-header">
                        <div class="key-title">
                            <span>${selectedKey}</span>
                            <span class="type-badge ${typeClass}">${typeLabel}</span>
                        </div>
                        <span class="ttl"><i class="far fa-clock"></i> <input type="number" id="detailTtl" value="${ttl}" title="TTL（秒），-1 表示永不过期"${ttlDisabled} /></span>
                        <div class="detail-actions">
                            <button class="primary" id="detailSave"${readonlyConn ? ' disabled' : ''} data-orig-title="保存修改" title="${readonlyConn ? '只读连接，禁止写操作' : '保存修改'}"><i class="fas fa-save"></i> 保存</button>
                            <button id="detailRefresh"><i class="fas fa-sync-alt"></i> 刷新</button>
                            <button class="danger" id="detailDelete"><i class="fas fa-trash-alt"></i> 删除</button>
                        </div>
                    </div>
```

- [ ] **Step 3: 修改保存逻辑以读取并提交 TTL**

将 `detailSave` 事件处理（约 2413-2431 行）替换为：

```js
                    const saveBtn = document.getElementById('detailSave');
                    if (saveBtn) {
                        saveBtn.addEventListener('click', () => {
                            if (!currentConn || !currentConn.id) {
                                showToast('请先连接 Redis 服务器', 'error');
                                return;
                            }
                            if (currentConn.readonly) {
                                showToast('只读连接，禁止写操作', 'error');
                                appendTerminal(`保存 Key "${selectedKey}" 失败: 当前为只读连接，禁止写操作`, 'error');
                                return;
                            }
                            const editor = document.getElementById('valueEditor');
                            if (!editor) {
                                showToast('此类型不支持直接修改', 'info');
                                return;
                            }
                            const value = editor.value;
                            const ttlInput = document.getElementById('detailTtl');
                            const ttlResult = parseTtl(ttlInput ? ttlInput.value : '-1');
                            if (!ttlResult.ok) {
                                showToast(ttlResult.message, 'error');
                                if (ttlInput) ttlInput.focus();
                                return;
                            }
                            saveBtn.disabled = true;
                            invoke('set_key', { connId: currentConn.id, key: selectedKey, value: value, ttl: ttlResult.ttl })
                                .then(() => {
                                    const ttlArg = ttlResult.ttl > 0 ? ` EX ${ttlResult.ttl}` : '';
                                    appendTerminal(`SET ${selectedKey}${ttlArg} → OK`, 'ok');
                                    showToast('Key 已保存', 'success');
                                    if (KEY_DATA[selectedKey]) {
                                        KEY_DATA[selectedKey].value = value;
                                    }
                                    loadKeys();
                                })
                                .catch(err => {
                                    showToast('保存失败: ' + err, 'error');
                                    appendTerminal(`保存 Key "${selectedKey}" 失败: ${err}`, 'error');
                                })
                                .finally(() => {
                                    saveBtn.disabled = false;
                                });
                        });
                    }
```

- [ ] **Step 4: 验证详情页 TTL 编辑**

在 Tauri 中打开一个已存在的 Key：
- 详情页 TTL 输入框显示当前 TTL（`-1` 或其他）。
- 修改为 `30`，点保存，终端显示 `SET key EX 30 → OK`。
- 刷新后 TTL 显示更新为 `30`。
- 输入非法值时保存被阻止并提示。

- [ ] **Step 5: 提交**

```bash
git add frontend/index.html
git commit -m "feat(ui): Key 详情页支持编辑 TTL"
```

---

## Task 4: 端到端验证与清理

**Files:**
- Modify: 无新文件，可能需修复 Task 1-3 遗漏的调用点。

- [ ] **Step 1: 全局搜索所有 `set_key` 调用**

Run:
```bash
grep -n "set_key" frontend/index.html src-tauri/src -R
```

Expected: 所有前端调用点都包含 `ttl` 参数；后端签名一致。

- [ ] **Step 2: 编译后端并启动 Tauri 验证**

Run:
```bash
cd /Users/yanshili/me/projects/myredis/src-tauri
cargo tauri dev
```

验证：
1. 新增 Key，TTL 默认 `-1`，成功创建。
2. 新增 Key，TTL `10`，创建后 `TTL key` 在终端执行返回约 `10`。
3. 编辑已有 Key 的 TTL，`30` 后保存并刷新，TTL 更新。
4. 输入 `0`、`-2`、`abc` 时前端拦截。
5. 只读连接下保存按钮与 TTL 输入框禁用。

- [ ] **Step 3: 运行 Rust 检查与测试**

Run:
```bash
cargo test
cargo clippy -- -D warnings
cargo fmt --check
```

Expected: 测试通过，无 Clippy 警告，无格式问题。

- [ ] **Step 4: 提交最终修正（如有）**

```bash
git add .
git commit -m "chore: TTL 输入框端到端验证与格式清理"
```

---

## 自我审查

### Spec 覆盖检查

| Spec 要求 | 对应任务 |
|-----------|----------|
| 新增 Key 对话框 TTL 输入框，默认 -1 | Task 2 |
| 详情页 TTL 可编辑 | Task 3 |
| 后端 `set_key` 原子化支持 TTL | Task 1 |
| TTL 单位为秒 | Task 1-3（使用 `EX`） |
| 仅允许 -1 或正整数 | Task 1（后端）+ Task 2（前端 `parseTtl`） |
| 兼容 Redis < 6.0 | Task 1（使用 `SET ... EX`，Redis 2.6.12+） |

### Placeholder 检查

- 无 "TBD" / "TODO"。
- 所有代码块包含完整代码。
- 所有命令包含预期输出。

### 类型一致性检查

- 后端 `set_key` 参数名为 `ttl`，类型 `i64`。
- 前端 invoke 调用均使用 `ttl: ttlResult.ttl`。
- 辅助函数 `parseTtl` 返回 `{ ok: boolean, ttl: number, message?: string }`。

无冲突。
